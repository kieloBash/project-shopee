"use client";
import React from "react";
import { CustomersIcon, UsersIcon } from "../icons";

const AddButton = () => {
  return (
    <button className="" type="button" onClick={() => {}}>
      <UsersIcon className="fill-accent-500" size={"xs"} />
    </button>
  );
};

export default AddButton;
